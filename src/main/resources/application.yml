server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/overlap
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    #type: com.alibaba.druid.pool.DruidDataSource
mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.ingram.overlap.bean.*
